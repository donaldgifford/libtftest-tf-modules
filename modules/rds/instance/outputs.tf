#--------------------------------------------------------------
# Module outputs (consumer contract)
#
# Stable surface; renaming or removing an output breaks downstream
# remote-state consumers. Populated in Phase 7: the instance contract
# plus the seven proxy-composition outputs (same names as serverless).
#--------------------------------------------------------------
