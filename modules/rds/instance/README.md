# modules/rds/instance

A single, non-clustered `aws_db_instance` for `postgres` / `mysql` workloads
that don't need Aurora (DESIGN-0012 / IMPL-0011).

See [`USAGE.md`](./USAGE.md) for the generated input/output reference. The
operator guide (examples, post-apply smoke recipe, gotchas) lands in Phase 10.
