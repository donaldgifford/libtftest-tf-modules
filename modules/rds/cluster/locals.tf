#--------------------------------------------------------------
# Computed locals
#
# Populated in Phase 2: KMS key ARN coalesce, the static Aurora
# engine+major -> parameter-family map, the default-major map, and
# the engine default-port map. Stubbed here so Phase 1 validates with
# no dangling references.
#--------------------------------------------------------------
