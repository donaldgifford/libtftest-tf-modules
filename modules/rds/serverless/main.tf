#--------------------------------------------------------------
# Data sources — caller identity + VPC remote state
#
# Caller identity is the ADR-0001 carve-out (read; do not parameterize).
# VPC remote state delivers vpc_id + private_subnet_ids per IMPL-0007 Q1.
# Wired in Phase 2.
#--------------------------------------------------------------
