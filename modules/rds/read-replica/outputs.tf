#--------------------------------------------------------------
# Module outputs (populated in Phase 4)
#
# Per-reader identifier + endpoint maps, keyed as var.replicas, for
# targeted routing. The cluster's own reader_endpoint remains the
# load-balanced entry point across all readers.
#--------------------------------------------------------------
