#--------------------------------------------------------------
# Upstream catalog and derived maps
#--------------------------------------------------------------
#
# Phase 2 lands local.upstream_catalog (six known upstreams) +
# local.selected (the caller-chosen subset) + local.authenticated
# (auth-required subset) + local.account_id (from
# data.aws_caller_identity.current).
